# RBAC module

**Purpose:** Resolves what a user may do. Phase 1 ships 4 fixed system roles (Owner, Manager, Driver, Farmer) enforced from stored role_permissions data — no role-builder UI until Phase 2 (§6).

**Status:** scaffold only — no routes/controllers/services/repositories implemented yet.

**Owns tables:** roles, permissions, role_permissions

**Spec reference:** docs/ShabooAgri_Goal_Specification.md
