<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Required for all network calls (login, sync, etc.) in every build
         variant. Only present in the debug/profile manifests by default
         (Flutter's stock template) — release builds don't merge those, so
         without this line here a release APK cannot make any network
         request at all, silently, at the OS level. -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.CAMERA"/>
    <application
        android:label="shabooagri_mobile"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
            <!-- App Links for the emailed auth deep links
                 (/accept-invite, /reset-password) on the ShabooAgri domain and
                 any tenant subdomain. autoVerify requires a matching
                 /.well-known/assetlinks.json hosted per host to auto-open
                 without a chooser (hosting + signing cert fingerprint are a
                 deploy step); the filter itself routes the URL into the app,
                 and Flutter deep-linking (below) hands it to go_router, which
                 has /accept-invite and /reset-password routes. -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>
                <data android:scheme="https"/>
                <data android:host="shabooagri.com"/>
                <data android:host="*.shabooagri.com"/>
                <data android:pathPrefix="/accept-invite"/>
                <data android:pathPrefix="/reset-password"/>
            </intent-filter>
        </activity>
        <!-- Enable Flutter's deep-link handling so incoming App Links are
             delivered to go_router instead of being ignored. -->
        <meta-data android:name="flutter_deeplinking_enabled" android:value="true"/>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
